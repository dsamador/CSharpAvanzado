using System;

namespace DelegadosTICapacitacion
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*Un delegado es un tipo de dato que 
             guarda direcciones de memoria de métodos 
             Un tipo delegado nos permite almacenar una
             o más direcciones de métodos
            */
            /*Cuando creamos un delegado debemos especificar
             el tipo de valor devuelto y la lista de parámetros de los métodos cuya dirección podrá almacenar 
            el tipo delegado. Entonces un tipo delegado sólo podrá almacenar direcciones de memoria de métodos
            que cumplan con la especificación declarada al definir el tipo delegado.*/

            /*Para definir un tipo delegado hacemos uso de la palabra delegate.
             Así como para crear un nuevo tipo necesitamos una clase en los delegados podemos crear variables
            del tipo del delegado. 27:42*/
            /*https://www.youtube.com/watch?v=_-98c1gaTfI&list=PLsi0_y2gnnl7fZIdXAjsBHmSuK27SNmxR*/
        }
    }
}
